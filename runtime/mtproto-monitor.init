#!/bin/sh /etc/rc.common

USE_PROCD=1
START=95
STOP=10

PROG=/usr/libexec/mtproto-monitor

start_service() {
	procd_open_instance
	procd_set_param command "$PROG" collect
	procd_set_param respawn 5 10 0
	procd_set_param stdout 1
	procd_set_param stderr 1
	procd_close_instance
}
